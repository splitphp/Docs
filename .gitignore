# Local Obsidian config — holds the semantic-vault-mcp data.json with the server's
# apiKey (seeded per instance: readOnlyMode, loopback, key). NEVER commit it: this repo
# is public and a pull must not clobber the server's config.
.obsidian/

# Obsidian trash
.trash/
